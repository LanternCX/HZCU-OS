执行状态: Active

# Lab07 作业计划

这份文档按串行步骤编排。

你从上往下做，每一步只会属于下面四类之一：

1. 运行命令
2. 修改文件
3. 截图
4. 回答问题

实验讲义按 Linux 环境设计，信号编号和 `sigqueue` 相关行为以 Linux 为准。建议在 Linux 虚拟机或实验环境中完成。

先进入实验目录：

```bash
cd "/Users/caoxin/Library/CloudStorage/OneDrive-Personal/Document/HZCU-OS/Lab07"
```

## 任务 1：通过键盘发送信号

### 步骤 1：修改文件

在 `SIGNAL1/signal1.c` 里确认这两处：

1. 保留 `ouch` 函数里的 `signal(SIGINT, SIG_DFL);`。
2. 在 `main` 里启用 `signal(SIGINT, ouch);`，不要启用 `signal(SIGINT, SIG_IGN);`。

### 步骤 2：运行命令

```bash
cd SIGNAL1
gcc signal1.c -o signal1
./signal1
```

程序运行后，先按一次 `Ctrl+C`，继续观察输出；再按第二次 `Ctrl+C`，让程序结束。

### 步骤 3：截图

1. 截一张编译成功图。
2. 第一次按 `Ctrl+C` 后截图，图里要能看到 `OUCH! - I got signal 2`，也要能看到程序继续输出 `Hello World!`。
3. 第二次按 `Ctrl+C` 后，如果终端回到命令行，再补一张程序结束图。

### 步骤 4：修改文件

为了观察删除语句①的变化，在 `SIGNAL1/signal1.c` 里临时注释掉 `ouch` 函数中的 `signal(SIGINT, SIG_DFL);`。

### 步骤 5：运行命令

```bash
gcc signal1.c -o signal1
./signal1
```

连续按两次 `Ctrl+C` 观察结果。观察完成后，如果程序没有退出，用 `Ctrl+\` 或关闭当前运行结束它。

### 步骤 6：截图

截一张删除语句①后的运行图，图里要能看出多次按 `Ctrl+C` 后程序仍然进入自定义处理。

### 步骤 7：修改文件

恢复 `ouch` 函数中的 `signal(SIGINT, SIG_DFL);`，再临时注释掉 `main` 里的 `signal(SIGINT, ouch);`。

### 步骤 8：运行命令

```bash
gcc signal1.c -o signal1
./signal1
```

程序运行后按一次 `Ctrl+C`。

### 步骤 9：截图

截一张删除语句②后的运行图，图里要能看出程序按默认方式结束，没有进入 `ouch` 的提示输出。

### 步骤 10：修改文件

把 `SIGNAL1/signal1.c` 恢复到任务开始时的状态：启用 `signal(SIGINT, ouch);`，并保留 `ouch` 函数中的 `signal(SIGINT, SIG_DFL);`。

### 步骤 11：回答问题

1. 发送信号：通过键盘 Ctrl-C 发送终止进程的信号。试通过键盘发送其他信号，观察程序是否有反应。
   第一次按 Ctrl-C 时，终端向前台进程发送 SIGINT，程序没有立即结束，而是进入预先安排的处理函数并输出提示。发送其他没有被程序专门处理的信号时，程序通常不会出现同样的自定义提示，而是按系统默认行为处理。
2. 预置对信号的处理方式：通过 signal 设置信号的处理方式。删除语句①或②，观察程序的变化。
   删除语句①后，SIGINT 的处理方式不会恢复为默认方式，所以每次按 Ctrl-C 都会继续进入 `ouch`。删除语句②后，程序没有注册自定义处理函数，按 Ctrl-C 时就直接按默认方式终止。
3. 接收信号的进程按事先规定完成对相应事件的处理：函数 ouch(int sig) 安排设置接收到信号后实施的动作。
   程序接收到 SIGINT 后会暂停原来的循环输出，转去执行 `ouch` 中安排的输出和恢复默认动作。处理完成后，如果没有终止，就回到原来的循环继续运行，这体现了信号处理的异步特点。

## 任务 2：闹钟 alarm 例程

### 步骤 1：修改文件

如果 Linux 编译器提示 `main` 缺少返回类型，就把 `SIGNAL1/signal3.c` 里的 `main()` 改成 `int main()`，并在函数末尾补上正常返回。

### 步骤 2：运行命令

```bash
gcc signal3.c -o signal3
./signal3
```

### 步骤 3：截图

1. 截一张编译成功图。
2. 运行到 `hello` 输出出现后截图，图里要能看到 `sleep 1 ...`、`sleep 2 ...` 以及定时触发后的 `hello`。

### 步骤 4：回答问题

1. 运行例程，观察输出结果。理解程序通过 alarm() 定时发送信号。
   程序先注册 SIGALRM 的处理函数，再通过 `alarm(3)` 安排 3 秒后发送一次 SIGALRM。运行时前几秒按循环正常输出，到达设定时间后，进程收到信号并执行处理函数，所以中间会出现 `hello`。

## 任务 3：setitimer 定时器例程

### 步骤 1：运行命令

```bash
gcc signal4.c -o signal4
./signal4
```

观察到多次 `SIGALRM` 和 `SIGVTALRM` 输出后，用 `Ctrl+C` 结束程序。

### 步骤 2：截图

1. 截一张编译成功图。
2. 截一张运行图，图里要能看到进程号，也要能看到 `Catch a signal -- SIGALRM` 和 `Catch a signal -- SIGVTALRM`。

### 步骤 3：回答问题

1. 了解 sys/time.h 中关于 struct itimerval 的定义。
   `struct itimerval` 里包含第一次触发时间和之后的间隔时间。例程分别设置了真实时间定时器和虚拟时间定时器，因此两个定时器可以按不同计时规则反复触发。
2. 了解定时器 ITIMER_REAL 和 ITIMER_VIRTUAL 的差异，理解实际时间和进程执行时间的概念。
   `ITIMER_REAL` 按现实经过的时间计时，不管进程是否一直占用 CPU。`ITIMER_VIRTUAL` 只按进程在用户态实际执行的时间计时。例程中程序一直空循环运行，所以能观察到两种信号都被触发。

## 任务 4：父子进程间通过 kill 发送信号

### 步骤 1：修改文件

在 `SIGNAL1/signal5.c` 中按 Linux 实验环境确认信号写法：

1. 子进程发送给父进程的信号使用 `SIGALRM`，不要直接依赖数字 `16`。
2. 父进程注册处理函数时使用 `SIGALRM`，不要直接依赖数字 `16`。
3. 如果编译器提示缺少等待函数声明，补充等待子进程所需的头文件。

### 步骤 2：运行命令

```bash
gcc signal5.c -o signal5
./signal5
```

### 步骤 3：截图

1. 截一张编译成功图。
2. 程序输出 `Ding!` 和 `done` 后截图，图里要能看到父进程等待、收到信号、继续执行的完整过程。

### 步骤 4：修改文件

为了观察取消语句①的变化，在 `SIGNAL1/signal5.c` 中临时注释掉 `pause();`，同时保持 `wait` 语句仍然注释。

### 步骤 5：运行命令

```bash
gcc signal5.c -o signal5
./signal5
```

### 步骤 6：截图

截一张取消语句①后的运行图，图里要能看出父进程没有等待信号处理完成就继续向后执行。

### 步骤 7：修改文件

继续保持 `pause();` 注释掉，同时启用 `wait(0);`。

### 步骤 8：运行命令

```bash
gcc signal5.c -o signal5
./signal5
```

### 步骤 9：截图

截一张取消语句①、保留语句②后的运行图，图里要能看到父进程等待子进程结束后再继续输出。

### 步骤 10：修改文件

把 `SIGNAL1/signal5.c` 恢复成正常实验版本：保留 `pause();`，`wait(0);` 可按讲义要求继续作为对比语句保留注释。

### 步骤 11：回答问题

1. 理解系统调用 kill() 和 signal() 的功能和使用方法。
   `signal()` 用来提前安排进程收到某个信号后执行什么动作，`kill()` 用来向指定进程发送信号。本例中子进程通过父进程号把信号发给父进程，父进程收到后执行 `ding`，从而改变后续输出。
2. 掌握父子进程获取对方进程号的方法。
   `fork()` 在父进程中返回子进程号，所以父进程可以知道子进程是谁；子进程可以通过 `getppid()` 获取父进程号，因此也能反向给父进程发送信号。
3. 取消语句①，观察输出的变化，解释原因。
   取消 `pause()` 后，父进程不会停下来等信号到来，而是直接检查变量并继续输出。由于子进程要延迟发送信号，父进程很可能在信号到来前就已经执行完后续语句，所以看不到正常的 `Ding!` 效果。
4. 取消语句①，保留语句②，观察输出的变化，解释原因。
   保留 `wait(0)` 后，父进程虽然不再用 `pause()` 等待信号，但会等待子进程结束。子进程结束前会发送信号，因此父进程仍然有机会完成信号处理，再继续输出后面的结果。

## 任务 5：进程使用信号通信

### 步骤 1：运行命令

```bash
gcc signal2.c -o signal2
./signal2
```

程序输出进程号后，分别按 `Ctrl+C` 和 `Ctrl+\` 观察结果。观察完成后，用终止信号结束程序。

### 步骤 2：截图

截一张运行图，图里要能看到进程号，以及收到 `SIGINT`、`SIGQUIT` 后的提示信息。

### 步骤 3：修改文件

新建 `signal-parent-child.c`，完成讲义“进程使用信号通信”例程：

1. 父进程捕捉键盘 `Ctrl+C`。
2. 父进程收到中断后，分别向两个子进程发送信号。
3. 两个子进程收到信号后分别输出提示信息并终止。
4. 父进程等待两个子进程终止后再输出结束信息。

### 步骤 4：运行命令

```bash
gcc signal-parent-child.c -o signal-parent-child
./signal-parent-child
```

程序运行后按一次 `Ctrl+C`。

### 步骤 5：截图

1. 新文件建好后截一张文件列表图。
2. 编译成功后截一张图。
3. 按 `Ctrl+C` 后截一张运行图，图里要能看到两个子进程和父进程的结束输出。
4. 如果出现两个子进程输出交替，再补一张能体现交替现象的图。

### 步骤 6：修改文件

如果要完成互斥控制要求，在 `signal-parent-child.c` 里增加对子进程输出顺序的控制，保证一个子进程的两行输出完成后，另一个子进程再输出。

### 步骤 7：运行命令

```bash
gcc signal-parent-child.c -o signal-parent-child
./signal-parent-child
```

程序运行后按一次 `Ctrl+C`。

### 步骤 8：截图

截一张互斥控制后的运行图，图里要能看出两个子进程的输出不再交替。

### 步骤 9：回答问题

1. 了解父进程利用 fork 的返回值，向子进程发送信号。
   父进程通过 `fork()` 的返回值保存两个子进程号，收到键盘中断后就能把不同信号分别发给对应子进程。子进程提前注册自己的处理方式，因此收到父进程信号后会退出等待并输出提示。
2. 观察下图各种可能的输出，尝试进行互斥控制，保证两个子进程的输出不交替。
   两个子进程同时被唤醒时，谁先输出由调度决定，所以原始结果可能交替。要保证不交替，就需要增加顺序控制，让一个子进程完整输出后再允许另一个子进程输出。

## 任务 6：使用 sigaction 注册信号

### 步骤 1：修改文件

在 `sigaction(1)/sigaction1.c` 中，如果编译器提示 `atoi` 没有声明，就补充所需头文件。

### 步骤 2：运行命令

```bash
cd ../sigaction\(1\)
gcc sigaction1.c -o sigaction1
./sigaction1 10
```

保持这个终端运行，另开一个终端进入同一目录，执行：

```bash
ps -a
kill -s 10 进程号
```

把 `进程号` 换成 `sigaction1` 对应的进程号。

### 步骤 3：截图

1. 截一张编译成功图。
2. 截一张 `ps -a` 和 `kill -s 10 进程号` 的发送信号图。
3. 回到运行 `sigaction1` 的终端，截一张收到信号后的图，图里要能看到 `receive signal 10`。

### 步骤 4：回答问题

1. 了解 sigaction 结构的定义。
   `sigaction` 结构用来描述一个信号的处理方式，包括处理函数、屏蔽集合和附加标志。本例设置了三参数处理函数和 `SA_SIGINFO`，为后续接收更多信号信息做准备。
2. 了解使用 sigaction() 函数注册信号。
   程序从命令行读取要处理的信号编号，再调用 `sigaction()` 把该信号和处理函数关联起来。注册完成后，进程收到对应信号时就会执行指定函数。
3. 理解信号注册，信号发送，信号处理函数三个重要环节。
   本例先在接收程序中完成注册，再通过另一个终端用 `kill` 发送信号，最后由处理函数输出接收结果。三步连起来就是信号通信的完整过程。

## 任务 7：使用 sigqueue 发送信号并传递附加信息

### 步骤 1：修改文件

在 `sigaction(1)/sigaction2.c` 中，如果编译器提示 `atoi` 没有声明，就补充所需头文件。该例程应在 Linux 环境中运行。

### 步骤 2：运行命令

```bash
gcc sigaction2.c -o sigaction2
./sigaction2 10
```

观察到多次输出后，用 `Ctrl+C` 结束程序。

### 步骤 3：截图

1. 截一张编译成功图。
2. 截一张运行图，图里要能看到 `wait for the signal`，也要能看到附加信息 `$` 被处理函数输出。

### 步骤 4：回答问题

1. 了解 union sigval 的定义。
   `union sigval` 是信号携带附加信息时使用的数据结构，可以传递整数，也可以传递指针。本例使用的是指针形式，把字符数组地址随信号一起传给处理函数。
2. 了解使用 sigqueue() 函数发送信号。
   `sigqueue()` 不只是发送信号编号，还可以同时附带一份参数。程序向自身发送信号后，处理函数能够从信号信息中取出这份参数并输出。
3. 观察进程给自身发送信号。
   该例程的发送者和接收者是同一个进程。它先注册处理函数，然后在循环中调用 `sigqueue()` 给自身发送信号，所以能周期性看到等待提示和处理输出交替出现。

## 任务 8：发送程序和接收程序分离

### 步骤 1：修改文件

在 `sigaction(1)/sigaction3r.c` 和 `sigaction(1)/sigaction3s.c` 中，如果编译器提示 `atoi` 没有声明，就补充所需头文件。两个程序都应在 Linux 环境中运行。

### 步骤 2：运行命令

在第一个终端运行接收程序：

```bash
gcc sigaction3r.c -o sigaction3r
gcc sigaction3s.c -o sigaction3s
./sigaction3r 10
```

保持接收程序运行，另开一个终端进入同一目录，先查找接收程序进程号：

```bash
ps -a
./sigaction3s 10 进程号
```

把 `进程号` 换成 `sigaction3r` 对应的进程号。

### 步骤 3：截图

1. 截一张两个程序都编译成功的图。
2. 截一张发送端运行命令图，图里要能看到发送时附加了信号编号和接收进程号。
3. 截一张接收端输出图，图里要能看到 `the int value is 8`。

### 步骤 4：回答问题

1. 理解发送进程和接收进程。
   接收程序先启动并注册信号处理函数，发送程序单独运行，根据命令行参数把信号发给接收程序。两个程序分别承担接收和发送角色，这比进程给自身发送信号更接近真实的进程间通信。
2. 按程序要求在运行时附加合适的参数，并运行检验。
   接收端需要附加信号编号，发送端需要附加信号编号和接收端进程号。参数正确时，接收端能够收到信号并输出随信号传来的整数值。

## 任务 9：编写程序 1，三代进程信号通信

### 步骤 1：修改文件

新建 `exercise1-three-process.c`，实现讲义编程题 1：

1. 父进程创建子进程。
2. 子进程再创建孙子进程。
3. 父进程捕捉键盘 `Ctrl+C`。
4. 父进程收到 `Ctrl+C` 后向子进程发送信号。
5. 子进程收到父进程信号后向孙子进程发送信号。
6. 孙子进程输出 `Grandson process1 is killed by son!` 后终止。
7. 子进程等待孙子进程终止后输出 `Child process is killed by parent!` 后终止。
8. 父进程等待子进程终止后输出 `Parent process is killed!` 后终止。

### 步骤 2：运行命令

```bash
gcc exercise1-three-process.c -o exercise1-three-process
./exercise1-three-process
```

程序运行后按一次 `Ctrl+C`。

### 步骤 3：截图

1. 新文件建好后截一张文件列表图。
2. 编译成功后截一张图。
3. 按 `Ctrl+C` 后截一张运行图，图里要能看到孙子进程、子进程、父进程按要求依次输出三行信息。

### 步骤 4：回答问题

1. 用 fork() 创建一个子进程，在子进程中再次调用 fork() 创建孙子进程。调用系统调用 signal()，让父进程捕捉键盘上来的中断信号；捕捉到中断信号后，父进程用系统调用 kill() 向子进程发出信号，子进程捕捉到信号后，调用 kill() 向孙子进程发出信号。
   这题的重点是让信号沿父进程、子进程、孙子进程逐级传递。父进程负责响应键盘中断，子进程负责转发信号，孙子进程负责最后响应；每一级等待下一级结束后再退出，就能得到题目要求的输出顺序。

## 任务 10：编写程序 2，父子进程互相发送信号

### 步骤 1：修改文件

新建 `exercise2-parent-child-loop.c`，实现讲义编程题 2：

1. 父进程创建子进程。
2. 父进程循环向子进程发送信号。
3. 子进程收到信号后输出 `child caught signal #次数`。
4. 子进程随后向父进程发送相同信号。
5. 父进程收到信号后输出 `parent caught signal #次数`。
6. 两边分别记录自己的全局计数，让输出能持续递增。
7. 为了方便截图，可以设置运行若干轮后正常结束。

### 步骤 2：运行命令

```bash
gcc exercise2-parent-child-loop.c -o exercise2-parent-child-loop
./exercise2-parent-child-loop
```

### 步骤 3：截图

1. 新文件建好后截一张文件列表图。
2. 编译成功后截一张图。
3. 截一张运行图，图里要能看到 `child caught signal #1`、`parent caught signal #1`，以及后续编号递增的结果。

### 步骤 4：回答问题

1. 父子进程间互相发送信号：父进程无限循环向子进程发送信号，子进程收到信号后向父进程发送相同信号，父子进程之间各自记录全局变量的变化。
   这个程序体现的是双向信号通信。父进程先发起一次通知，子进程收到后再把同类信号发回父进程，两边各自更新计数，因此输出会以父子交替响应的形式持续递增。

## 任务 11：编写程序 3，用 sigaction 和 sigqueue 重做三代进程通信

### 步骤 1：修改文件

新建 `exercise3-three-process-sigqueue.c`，把任务 9 的三代进程通信改为使用 `sigaction()` 和 `sigqueue()`：

1. 父进程、子进程、孙子进程都用 `sigaction()` 注册信号处理方式。
2. 父进程捕捉键盘 `Ctrl+C` 后，用 `sigqueue()` 向子进程发送信号和附加信息。
3. 子进程收到后，用 `sigqueue()` 向孙子进程继续发送信号和附加信息。
4. 孙子进程收到后输出附加信息和题目要求的结束信息。
5. 子进程、父进程按任务 9 的要求等待并输出结束信息。

### 步骤 2：运行命令

```bash
gcc exercise3-three-process-sigqueue.c -o exercise3-three-process-sigqueue
./exercise3-three-process-sigqueue
```

程序运行后按一次 `Ctrl+C`。

### 步骤 3：截图

1. 新文件建好后截一张文件列表图。
2. 编译成功后截一张图。
3. 按 `Ctrl+C` 后截一张运行图，图里要能看到信号附加信息，也要能看到三代进程按要求结束。

### 步骤 4：回答问题

1. 将编程题 1 用 sigaction() 和 sigqueue() 再实现一次，并在信号传递时附带上信息。
   这题是在任务 9 的通信流程上增加附加信息传递。`sigaction()` 负责让处理函数能够读取信号信息，`sigqueue()` 负责在发送信号时带上参数，因此报告中应重点展示三代进程既能按顺序结束，也能正确接收并输出附加信息。
