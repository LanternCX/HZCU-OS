# 实验一 进程的创建和简单控制

## 实验目的

1. 理解系统调用的概念；
2. 认识进程的并发执行，了解进程族之间各种标识及其存在的关系；
3. 熟悉进程的创建、阻塞、唤醒、撤销等控制方法。

## 实验内容

1. 掌握进程创建的系统调用 `fork()`；
2. 了解并发程序的不可确定性，进行简单并发程序设计；
3. 使用系统调用：进程的创建 `fork()`、阻塞 `wait()`、睡眠 `sleep()`、终止 `exit()` 等。

## 实验步骤

### （一）系统调用

系统功能调用（system call）是操作系统提供给程序设计人员的一种服务。程序设计人员在编写程序时，可以利用系统调用来请求操作系统的服务。

### （二）本实验涉及的系统调用

#### 1. 创建一个新进程：`pid_t fork(void);`

函数说明：

`pid_t` 是一个宏定义，其实质是 `int`，被定义在 `#include <sys/types.h>` 中。

系统调用 `fork` 用于创建一个新进程。调用者称为父进程，生成的新进程称为子进程。创建新进程后，父子两个进程将执行 `fork()` 系统调用之后的下一条指令。子进程使用相同的 PC（程序计数器）、相同的 CPU 寄存器，以及相同的打开文件。

`fork` 调用的一个奇妙之处就是它仅仅被调用一次，却能够返回两次，它可能有三种不同的返回值：

1. 在父进程中，`fork` 返回新创建子进程的进程 ID；
2. 在子进程中，`fork` 返回 `0`；
3. 如果出现错误，`fork` 返回一个负值。

需要的头文件：

1. `#include <unistd.h>`
2. `#include <sys/types.h>`

#### 2. 获取进程标识号：`pid_t getpid(void); pid_t getppid(void);`

函数说明：

系统调用 `getpid()` 用来取得当前进程的进程 ID，系统调用 `getppid()` 用来取得当前进程的父进程 ID。

返回值：

`getpid()` 返回当前进程的进程 ID；`getppid()` 返回当前进程的父进程 ID。

#### 3. 等待子进程终止：`pid_t wait(int *status);`

函数说明：

系统调用 `wait()` 用于使父进程阻塞，直到一个子进程结束或者该进程接收到了一个指定的信号为止。如果该父进程没有子进程或者它的子进程已经结束，则 `wait()` 就会立即返回。

返回值：

成功返回已运行结束的子进程号；失败返回 `-1`。

需要的头文件：

1. `#include <sys/wait.h>`
2. `#include <sys/types.h>`

#### 4. 终止进程：`void exit(int status);`

函数说明：

使调用本函数的进程正常终止，然后把形参的值 `status&0377`（八进制）返回给父进程，父进程可以通过 `wait` 函数族来获取这个返回值。

`exit(status)` 函数执行之后，形参的值会被传递到父进程，这时有 3 种情况：

1. 如果父进程设置了 `SA_NOCLDWAIT` 标志，或者把 `SIGCHLD` 信号的处理函数设置为 `SIG_IGN`，那么子进程的返回值被丢弃，子进程立即消亡；
2. 如果父进程在等待子进程，他将被通知子进程的退出状态；子进程立即消亡；
3. 如果父进程没有被设置为“忽略子进程的退出值”，这时父进程应当使用 `wait` 或者 `waitpid` 等待子进程的结束，如果父进程不等待，那么子进程结束之后会变成僵尸进程。

### （三）例程，使用 `fork()` 创建进程

#### 1. 编辑下述代码，详见教材 P98。

```c
#include<stdio.h>

main()
{
 int pid;
 pid = fork();
 printf("pid= %d\n",pid);                     
 //printf("pid= %d\n",getpid());                
 sleep(1);
}
```

编译链接通过后，多次运行例程，观察进程并发执行结果，并思考下述问题：

1. 为什么会有两行输出？理解 `fork()` 的作用；
2. 多次运行，观察输出内容的变化，理解系统给进程随机分配进程号；
3. 将语句  替换成语句 ，再次运行程序。观察输出的改变，理解 `fork()` 的返回值。

参考回答：

1. 因为执行 `fork()` 之后，程序会分成父进程和子进程两个执行分支，而 `printf` 位于 `fork()` 之后，所以父子两个进程都会执行这条打印语句，因此会出现两行输出。
2. 因为系统每次创建子进程时分配的进程号都可能不同，所以多次运行时输出的数字会发生变化。
3. 语句  打印的是 `fork()` 的返回值，父进程打印的是子进程的进程号，子进程打印的是 `0`；语句  打印的是当前正在运行的进程自己的进程号，所以替换之后会看到两个不同的真实进程号，从而更容易区分“返回值”和“进程自身编号”的区别。

#### 2. 编辑下面的程序，要求实现父进程产生两个子进程，父进程显示字符 “a”、两个子进程分别显示字符 “b”、“c”，如下所示。

```c
#include<stdio.h>
main( )
{
 int p1,p2;

 while ((p1 = fork( )) == -1);        // 父进程创建第一个进程，直到成功
 if(p1 == 0)                      //0返回给子进程 1
  {
   putchar('b');             //P1 的处理过程
   }
 else               

 {
  while ((p2 = fork( )) == -1);    // 父进程创建第二个进程，直到成功
  if(p2 == 0)                  //0 返回给子进程2
   {
   putchar('c');          //P2的处理过程
   }
  else               
   {
   putchar('a');          //P2 创建完成后，父进程的处理过程
   }
 }
}
```

编译链接通过后，多次运行例程，观察进程并发执行结果，并思考下述问题：

1. 分析为何例程中三个分支都运行了？
2. `./f1` 运行结果为什么不一样？每种结果的产生原因。
3. 删除语句  或 ，观察输出的内容，体会 `fork` 的使用。
4. 运行命令为什么是“`./command`”？理解 Linux 的 `PATH` 环境变量的作用。
5. `.` 和 `..` 什么含义？理解 Linux 当前目录和父目录的概念。
6. shell 提示为什么不换行，而是紧接着输出内容显示？
7. 输出字母为什么和提示交错？
8. `./f1|pstree|grep f1` 什么含义？理解命令中管道的作用和使用方法。
9. 第 8 问中有时组合命令没有输出，请分析原因？
10. 如果想保留第 8 问的 `./f1` 的输出内容，该如何操作？理解 Linux 文件重定向的概念和使用方法。

扩展编程：修改代码，产生祖孙三代的进程。

参考回答：

1. 因为父进程先执行第一次 `fork()`，产生第一个子进程，子进程输出 `b`；父进程随后继续执行第二次 `fork()`，再产生第二个子进程，第二个子进程输出 `c`，父进程自己输出 `a`，所以三个分支都会运行。
2. 因为三个进程是并发执行的，系统每次调度它们的先后顺序都可能不同，所以运行结果不完全一样，例如可能出现 `abc`、`acb` 等不同顺序。
3. 删除语句  或  后，会改变哪些进程还能继续往下执行，从而导致输出字符的种类或个数发生变化。也就是说，这两个位置的判断语句起到了隔离不同进程执行路径的作用。
4. 因为当前目录默认不一定在 `PATH` 里，所以要写 `./command` 来明确表示运行当前目录下的可执行文件。
5. `.` 表示当前目录，`..` 表示当前目录的上一级目录。
6. 因为程序里只输出了字符，没有输出换行符，所以 shell 的提示符会直接跟在输出后面显示。
7. 因为程序输出和 shell 提示都显示在同一个终端里，而且程序本身没有换行，所以会出现交错现象。
8. 这条命令的含义是：先运行 `./f1`，再把前一个命令的输出通过管道交给后一个命令继续处理，最后用 `grep f1` 只筛出和 `f1` 有关的内容。
9. 因为 `f1` 运行时间很短，相关进程可能在后续命令处理之前就已经结束了，所以有时看不到输出。
10. 可以使用输出重定向把程序结果保存到文件中，例如把 `./f1` 的输出写入一个文本文件，再单独查看该文件内容。

#### 3. 扩充：关于父子进程各自又再生成子进程的例子。

```c
#include <unistd.h>
#include <stdio.h>
#include <sys/types.h>

main()


   {
       pid_t    a_pid,b_pid;
       if((a_pid=fork())<0)
          printf("error!");
       else
         if(a_pid==0)
             printf("b\n");
         else
             printf("a\n");

       if((b_pid=fork())<0)
          printf("error!");
       else
          if(b_pid==0)
             printf("c\n");
          else
             printf("a\n");
       }
```

编译链接通过后，多次运行例程，观察进程并发执行结果，并思考下述问题：

1. 例程运行后，共产生了几个进程？请分析出它们的宗族关系。
2. 例程运行后，共输出几个字符？分别是什么字符？分别由哪个进程输出的？
3. 删除输出语句中的回车符，输出结果有何改变？试分析原因，理解输出缓冲的概念。

参考回答：

1. 例程运行结束后，一共会有 4 个进程。最开始只有 1 个原始进程；第一次 `fork()` 之后变成 2 个进程；接着这 2 个进程都会继续执行第二次 `fork()`，所以最后一共形成 4 个进程。
2. 一共会输出 6 个字符。第一次 `fork()` 后，原父进程输出一个 `a`，第一个子进程输出一个 `b`；第二次 `fork()` 时，原来的两个进程都会继续执行 `else` 分支各输出一个 `a`，新产生的两个子进程各输出一个 `c`。因此总字符组成是 3 个 `a`、1 个 `b`、2 个 `c`，只是顺序不固定。
3. 删除回车符后，输出结果会更容易连在一起，不再一行一个字符。这是因为输出先进入缓冲区，多个进程的输出混在一起后再一起显示，因此更容易看到交错和粘连现象。

#### 4. 理解系统调用 `wait()`、`getpid()` 和 `getppid()` 的使用。程序代码如下所示。

```c
#include <stdio.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <unistd.h>
#include <errno.h>

int main()
{
 char buf[100];
 pid_t cld_pid;
 int fd;

 if((fd=open("temp",O_CREAT|O_TRUNC|O_RDWR,S_IRWXU))==-1)

 {
  printf("open error%d",errno);
  exit(1);
 }
 strcpy(buf,"This is parent process write\n");

 if((cld_pid=fork())==0)
 {
  strcpy(buf,"This is child process write\n");
  printf("This is child process\n");
  sleep(1);
  printf("My PID (child) is%d\n",getpid());
  sleep(1);
  printf("My parent PID is %d\n",getppid());
  sleep(1);
  write(fd,buf,strlen(buf));
  close(fd);
  exit(0);
 }
 else
 {
  wait(0);
  printf("This is parent process\n");
  sleep(1);
  printf("My PID (parent) is %d\n",getpid());
  sleep(1);
  printf("My child PID is %d\n",cld_pid);
  sleep(1);
  write(fd,buf,strlen(buf));
  close(fd);
 }
 return 0;
}
```

编译链接通过后，多次运行例程，观察进程并发执行结果，并思考下述问题：

1. 请分析父子进程输出内容交替的原因。
2. 语句 `sleep(1);` 起什么作用？删除所有 `sleep(1);` 语句，并观察运行结果；
3. 删除 `wait(0);` 语句，并观察运行结果，并请分析两次结果不同的原因，理解 `wait` 的作用。
4. 理解上图中两次运行后，子进程输出的差异，理解孤儿进程的概念。

参考回答：

1. 父子进程是并发执行的，系统会轮流给它们分配运行时间，所以输出内容可能交替出现。
2. `sleep(1)` 的作用是人为拉开输出时间，便于观察父子进程的执行顺序。删除后，输出会更紧凑、更容易交错在一起。
3. `wait(0)` 的作用是让父进程先等待子进程结束，再继续执行自己的后续内容。删除之后，父进程和子进程的先后关系不再受控制，所以输出结果会更乱，也更容易出现父进程先结束的情况。
4. 如果父进程先结束，而子进程还在运行，那么子进程之后再调用 `getppid()` 时，看到的父进程编号就可能发生变化。这说明原父进程已经退出，子进程被系统接管，这种子进程就称为孤儿进程。

#### 5. 编辑运行下述程序，观察孤儿进程。

孤儿进程：一个父进程退出，而它的一个或多个子进程还在运行，那么那些子进程将成为孤儿进程。孤儿进程将被 `init/systemd` 进程（进程号为 1）所收养，并由 `init` 进程对它们完成状态收集工作。孤儿进程不会浪费资源。

```c
#include <stdio.h>
#include <stdlib.h>
#include <errno.h>
#include <unistd.h>

int main()
{
    pid_t pid;

    pid = fork();      // 创建一个进程
    if (pid == 0)      //子进程
    {
       printf("child:I am the child process.\n");
       printf("child:pid: %d\tppid:%d\n", getpid(), getppid());//输出进程 ID 和父进程 ID
       printf("child:I will sleep for five seconds.\n");
       sleep(5);//睡眠 5s，保证父进程先退出
       printf("child:pid: %d\tppid:%d\n", getpid(), getppid());
       printf("Child process exited.\n");
     }
     else//父进程
     {
       printf("I am the father process.\n");
       sleep(1);//父进程睡眠 1s，保证子进程输出进程 id
       printf("Father process exited.\n");
     }
     exit(0);
}
```

编译链接通过后，多次运行例程，观察进程并发执行结果，并思考下述问题：

1. 删除所有 `sleep()`，观察父子进程的宗族关系及各自的进程号；
2. 恢复所有 `sleep()`，观察父进程提前结束后，子进程成为孤儿进程转交给 1 号进程。

参考回答：

1. 删除所有 `sleep()` 后，父子进程的执行先后会更不稳定，父进程不一定会先结束，因此子进程前后两次看到的父进程编号未必有明显变化。
2. 恢复 `sleep()` 后，父进程会先退出，而子进程继续运行。此时子进程再次打印父进程编号时，通常会看到父进程编号变成 1，说明它已经成为孤儿进程并被 1 号进程接管。

#### 6. 编辑运行下述例程，观察僵尸进程。

僵尸进程：一个进程使用 `fork` 创建子进程，如果子进程退出，而父进程并没有调用 `wait` 或 `waitpid` 获取子进程的状态信息，那么子进程的进程描述符仍然保存在系统中。这种进程称之为僵尸进程。僵尸进程浪费系统资源（进程描述符 `task_struct` 存在）。

```c
#include <stdio.h>
#include <unistd.h>
#include <errno.h>
#include <stdlib.h>

 int main()
{
     pid_t pid;
     pid = fork();
   if (pid == 0)
     {
         printf("I am the child process. I existed.\n");
         exit(0);
     }
     printf("I am the father process.I will sleep for two seconds\n");


     //等待子进程先退出
     sleep(2);
     //输出进程信息
     system("ps -o pid,ppid,state,tty,command");
     printf("Father process exited.\n");
     exit(0);
 }
```

编译链接通过后，多次运行例程，观察进程并发执行结果，并思考下述问题：

1. 理解 `system()`，在程序中运行 shell 命令；
2. 观察输出内容，查看僵尸进程。

参考回答：

1. `system()` 的作用是在程序内部临时调用一条 shell 命令，因此这里可以直接在程序运行过程中执行 `ps` 命令并显示结果。
2. 子进程已经执行 `exit(0)` 结束，但父进程还没有调用 `wait()` 回收它的状态信息，所以在 `ps` 的结果中仍然可以看到它的记录，这就是僵尸进程现象。

### （四）编程题：理解前述例程后，按要求完成程序编写。

编写程序创建子进程。父子进程分别打印自己和父进程的进程 ID，要求每 3 秒钟打印系统进程信息，重复 5 次后退出。父进程待子进程结束后退出。提示：

1. 用系统调用 `getpid` 和 `getppid` 获取进程 ID；
2. 用系统调用 `fork` 进程创建；
3. 用系统调用 `wait` 控制父子进程同步；
4. 用库函数 `system` 实现在一个进程内部运行另一个进程，即创建一个新进程；
5. Shell 命令 `"/bin/ps"` 作为 `system` 的字符串参数，实现打印系统进程信息。
